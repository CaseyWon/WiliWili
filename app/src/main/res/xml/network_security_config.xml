<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Bilibili APIs — all pin to bilibili subdomains only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow plaintext for local debug proxy (Charles, mitmproxy, etc.) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
